#include <stdio.h>
#include <stdlib.h>
#include <malloc.h>
#include <time.h>

void minimoFilas(int **p, *v)

int main() {
  int **p, *v, n, m;
  srand(time(NULL));
  
  v = (int*) malloc (n * sizeof(int));
  p = (int**) malloc (n * sizeof(int*));
  p[i] = (int*) malloc (m * sizeof(int));

  for (int i = 0; i < n; i++) {
    for (int j = 0; j < m; j++) {
      p[i][j] = rand() % 21 - 10;
    }
  }

  
}
